# Arquivos do sistema
Thumbs.db
.DS_Store

# Variáveis de ambiente / segredos (prevenção — nenhum arquivo desse tipo
# existe hoje no projeto, mas evita commit acidental no futuro)
.env
.env.*
*.env
.env.local
